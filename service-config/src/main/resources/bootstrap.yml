server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/willian-kaminski/microservice-springboot
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
